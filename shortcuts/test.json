{
  "shortcuts": [
    {
      "alias": "init",
      "description": "Push to github using `deploy \"Initial Commit\"`",
      "command": [
        "git init",
        "git add .",
        "git commit -m %*",
        "git push origin master"
      ]
    }
  ]
}
